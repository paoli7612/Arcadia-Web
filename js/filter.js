var filterDecors = function(checked){
  var decors = $("img.decor");
  if (checked){ decors.show(); }
  else { decors.hide(); }
}

var filterCartels = function(checked){
  var decors = $("img.cartel");
  if (checked){ decors.show(); }
  else { decors.hide(); }
}
